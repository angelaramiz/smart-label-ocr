package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// === PRIMARY: Teal palette (balanced, not too bright, not too dark) ===
val PrimaryTeal = Color(0xFF0D9488)       // teal-600 — main accent
val PrimaryTealLight = Color(0xFFF0FDFA)  // teal-50 — containers, chips
val PrimaryTealDark = Color(0xFF0F766E)   // teal-700 — pressed/active
val PrimaryTealMid = Color(0xFF14B8A6)    // teal-500 — secondary highlights
val PrimaryTealContainer = Color(0xFFCCFBF1) // teal-100

// === BACKGROUNDS: Warm Stone palette ===
val SurfaceWarm = Color(0xFFFAFAF9)       // stone-50  — screen background
val SurfaceCard = Color(0xFFFFFFFF)       // pure white — cards
val SurfaceOverlay = Color(0xFFF5F5F4)    // stone-100 — input backgrounds

// === BORDERS ===
val BorderSoft = Color(0xFFE7E5E4)        // stone-200
val BorderMuted = Color(0xFFD6D3D1)       // stone-300

// === TEXT ===
val TextPrimary = Color(0xFF1C1917)       // stone-900
val TextSecondary = Color(0xFF78716C)     // stone-500
val TextMuted = Color(0xFFA8A29E)         // stone-400
val TextOnPrimary = Color(0xFFFFFFFF)

// === SEMANTIC ===
val SuccessGreen = Color(0xFF059669)      // emerald-600
val SuccessContainer = Color(0xFFD1FAE5)  // emerald-100
val ErrorRose = Color(0xFFE11D48)         // rose-600
val ErrorContainer = Color(0xFFFFE4E6)    // rose-100
val WarningAmber = Color(0xFFD97706)      // amber-600
val WarningContainer = Color(0xFFFEF3C7) // amber-100

// === ZEBRA / TABLE ROWS ===
val ZebraRow = Color(0xFFF5F5F4)          // stone-100

// === DARK THEME (balanced, not pitch-black) ===
val DarkBackground = Color(0xFF18181B)    // zinc-900
val DarkSurface = Color(0xFF27272A)       // zinc-800
val DarkSurfaceVariant = Color(0xFF3F3F46) // zinc-700
val DarkBorder = Color(0xFF52525B)        // zinc-600
val DarkTextPrimary = Color(0xFFF4F4F5)   // zinc-100
val DarkTextSecondary = Color(0xFFA1A1AA) // zinc-400
